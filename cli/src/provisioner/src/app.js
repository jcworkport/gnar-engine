import { logger } from '@gnar-engine/core';


/**
 * Initialise service
 */
export const initService = async () => {

	logger.info('G n a r  E n g i n e | Provisioner provisioned.');
}

initService();
